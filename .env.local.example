# Copia questo file in ".env.local" (che NON viene versionato) e riempi i valori.

# Personal Access Token GitHub dell'account marabellis-prog, usato SOLO da
# scripts/rilascia.mjs per pubblicare le release (aggiornamenti automatici).
# Permessi necessari: repo (classic) oppure Contents read/write (fine-grained).
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
